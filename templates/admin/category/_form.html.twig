<div class="card-body">

{{ form_start(form) }}
    {{ form_widget(form) }}
    </div>
        <div class="card-footer">
    <button class="btn  btn-primary">{{ button_label|default('Add') }}</button>
{{ form_end(form) }}
        </div>
