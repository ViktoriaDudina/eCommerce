<aside class="main-sidebar sidebar-light-primary elevation-4">
    <!-- Brand Logo -->
    <a href="index3.html" class="brand-link">
      <span class="brand-text font-weight-light">Admin Panel</span>
    </a>

    <!-- Sidebar -->
    <div class="sidebar">
      <!-- Sidebar user panel (optional) -->
      <div class="user-panel mt-3 pb-3 mb-3 d-flex">
        
        <div class="info">
          <a href="#" class="d-block">Admin</a>
        </div>
      </div>

     

      <!-- Sidebar Menu -->
      <nav class="mt-2">
        <ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu" data-accordion="false">
          <!-- Add icons to the links using the .nav-icon class
               with font-awesome or any other icon font library -->
               <li class="nav-item">
            <a href="{{path('app_admin')}}" class="nav-link {{ app.request.attributes.get('_route') == 'app_admin' ? 'active' }}">
              <i class="nav-icon fas fa-th"></i>
              <p>
                Dashboard
                {# {{-- <span class="right badge badge-danger">New</span> --}} #}
              </p>
            </a>
          </li>
          <li class="nav-item menu-open">
            <a href="#" class="nav-link ">
              <i class="nav-icon fas fa-tachometer-alt"></i>
              <p>
                Products
                <i class="right fas fa-angle-left"></i>
              </p>
            </a>
            <ul class="nav nav-treeview">
              <li class="nav-item">
                <a href="{{path('app_products_index')}}" class="nav-link {{ app.request.attributes.get('_route') starts with 'app_products' ? 'active' }}">
                  <i class="far fa-circle nav-icon"></i>
                  <p>Products</p>
                </a>
              </li>
              <li class="nav-item">
                <a href="{{path('app_category_index')}}" class="nav-link {{ app.request.attributes.get('_route') starts with 'app_category' ? 'active' }}">
                  <i class="far fa-circle nav-icon"></i>
                  <p>Categories</p>
                </a>
              </li>
            </ul>
          </li>
          <li class="nav-item">
            <a href="{{path('app_brands_index')}}" class="nav-link {{ app.request.attributes.get('_route') starts with 'app_brands' ? 'active' }}">
              <i class="nav-icon fas fa-th"></i>
              <p>
                Brands
                {# {{-- <span class="right badge badge-danger">New</span> --}} #}
              </p>
            </a>
          </li>
          <li class="nav-item">
            <a href="{{path('app_user_index')}}" class="nav-link {{ app.request.attributes.get('_route') starts with 'app_user' ? 'active' }}">
              <i class="nav-icon fas fa-th"></i>
              <p>
                Users
                {# {{-- <span class="right badge badge-danger">New</span> --}} #}
              </p>
            </a>
          </li>
          <li class="nav-item">
            <a href="{{path('app_review_index')}}" class="nav-link {{ app.request.attributes.get('_route') starts with 'app_review' ? 'active' }}">
              <i class="nav-icon fas fa-th"></i>
              <p>
                Reviews
                {# {{-- <span class="right badge badge-danger">New</span> --}} #}
              </p>
            </a>
          </li>

        </ul>
      </nav>
      <!-- /.sidebar-menu -->
    </div>
    <!-- /.sidebar -->
  </aside>
