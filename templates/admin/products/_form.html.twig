<div class="card-body">

{{ form_start(form) }}
    {{ form_widget(form, {'attr': {'class': 'row g-3'}}) }}
     </div>
        <div class="card-footer">
    <button class="btn btn-primary">{{ button_label|default('Add') }}</button>
{{ form_end(form) }}
        </div>
